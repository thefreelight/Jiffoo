# Test Database URL (Required)
# Use a separate database for testing to avoid data loss
DATABASE_URL_TEST="postgresql://postgres:postgres@localhost:5432/jiffoo_mall_test?schema=public"

# JWT Secret for signing test tokens
JWT_SECRET="test-secret-key-for-testing"

# Server Port (Optional, random port used if not specified)
# PORT=3001

# Logging Level
LOG_LEVEL="error"
