/**
 * Order Schemas
 *
 * NOTE: Backend uses its own Zod schemas in apps/api/src/core/order/types.ts.
 * This file is intentionally kept empty to avoid drift.
 * Frontend validation (if needed) should be done inline or imported from
 * the backend schema definitions via a shared contract.
 */

export {};
